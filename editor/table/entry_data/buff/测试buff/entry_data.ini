-- ========================================================================
-- 【实例模块[buff.测试buff]配置文件】
-- 说明：
--   1、#NIL代表强制赋值为nil，无视继承值。
--   2、#CONFIG代表当前配置文件对应实例模块自身的配置。
-- ========================================================================

-- ************************************************************************
[#CONFIG]
'Version' = 13
-- ************************************************************************

-- ************************************************************************
-- [$$p_wbud.buff.测试buff.root]
['root']
'Version' = 1
'NodeType' = '$$.buff.Buff'
'Inherit' = '$$.template@buff.Buff.root'
'Data' = {
    'Editor' = {
    },
    'Game' = {
        'Name' = '[EntryNode][$$p_wbud.buff.测试buff.root].Data.Game.Name',
        'KeyValuePairs' = {
            1 = {
                'Key' = '生命上限',
                'Value' = {
                    'IsFunc' = false,
                    'Value' = -20,
                    'LuaCode' = '(function ()\r\n        local func = function (EffectParam)\r\n            return -20\r\n        end\r\n        return func\r\n    end)()',
                },
                'Random' = 30,
                'Percentage' = false,
            },
        },
    },
}
'UIConfig' = {
}
-- ************************************************************************